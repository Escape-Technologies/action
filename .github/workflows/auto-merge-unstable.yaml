name: Auto merge v0

on: 
  push:
    tags:
      - 'v0.*'

jobs:
  merge-release-v0:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set Git config
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "Github Actions"

      - name: Auto merge v0
        run: |
          git fetch --unshallow
          git checkout main
          git pull
          git merge --no-ff v0 -m "Auto-merge v0"
          git push
